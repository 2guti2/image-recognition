RSpec.describe Aws::Image::Recognition do
  it "has a version number" do
    expect(Aws::Image::Recognition::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
